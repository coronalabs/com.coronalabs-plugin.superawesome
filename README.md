Sources for the plugin `plugin.superawesome`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.superawesome" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
